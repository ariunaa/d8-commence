{% set heading_id = attributes.id ~ '-menu'|clean_id %}
<nav role="navigation" aria-label="site wide" {{ attributes|without('role', 'aria-labelledby') }}>
  {% block content %}
    {{ content }}
  {% endblock %}
</nav>
